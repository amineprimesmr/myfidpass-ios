# Artefacts locaux lourds / temporaires (ne pas versionner — clones plus légers).
.venv_motion/
.tmp_motion_frames/
.agents/

.DS_Store

# AAB Play Store (généré par scripts/prepare-play-upload.sh)
PlayStoreMetadata/releases/

# Xcode utilisateur (évite conflits Git / bruit).
**/xcuserdata/
*.xcuserstate
